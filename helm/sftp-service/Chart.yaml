apiVersion: v2
name: sftp-service
description: SFTP service (Go) with Vault-backed users, per-user jail, quotas, audit logs, and Prometheus metrics
type: application
version: 0.1.0
appVersion: "0.1.0"
