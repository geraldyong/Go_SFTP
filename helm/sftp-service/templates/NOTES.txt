Installed {{ .Release.Name }} in namespace {{ .Release.Namespace }}

SFTP service:
  kubectl -n {{ .Release.Namespace }} get svc {{ include "sftp.fullname" . }}-sftp

Admin API:
  kubectl -n {{ .Release.Namespace }} get svc {{ include "sftp.fullname" . }}-admin-api

Web UI:
  kubectl -n {{ .Release.Namespace }} get svc {{ include "sftp.fullname" . }}-web-ui

Vault dev:
  kubectl -n {{ .Release.Namespace }} get svc {{ include "sftp.vaultSvc" . }}

To port-forward SFTP:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "sftp.fullname" . }}-sftp 2022:2022

