{{- if .Values.seed.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sftp.fullname" . }}-seed
  labels:
    {{- include "sftp.labels" . | nindent 4 }}
data:
  alice.pub: {{ .Values.seed.alicePublicKey | quote }}
{{- end }}

